---
import '~styles/global.css';
import Head, { type HeadProps } from '~components/Head.astro';

export interface Props {
  meta?: Partial<HeadProps>;
}
---

<!DOCTYPE html>
<html lang="en">
  <Head></Head>
</html>
